import React from "react";

// Execute code button
const ExecuteCodeBtn = (props) => {
    return <button className="ExecuteCodeBtn" onClick={props.executeCode} >Execute code</button>;
};

export default ExecuteCodeBtn;
